kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 31693
    hostPort: 31693
    listenAddress: "0.0.0.0"  # Позволяет доступ со всех интерфейсов
    protocol: tcp
